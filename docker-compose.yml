services:
  api:
    build: .
    ports:
      - "8900:8900"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
    restart: unless-stopped
